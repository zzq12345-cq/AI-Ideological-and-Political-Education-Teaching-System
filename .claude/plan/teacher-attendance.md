# 教师考勤功能规划文档

> 老王说：这他妈是个实用功能！考勤嘛，简单记录谁来了谁没来，别搞得太复杂。

## 1. 目标定义

### 1.1 核心目标
- 为教师提供便捷的学生考勤管理功能
- 支持按班级、按课次进行考勤记录
- 提供考勤统计和分析报表
- 与现有的学情分析模块数据打通

### 1.2 用户场景
1. **上课点名**：教师选择班级和课程，快速完成点名
2. **补录考勤**：对之前的课程进行考勤补录
3. **查看历史**：查看某个班级/学生的考勤历史记录
4. **统计分析**：查看出勤率统计、缺勤排行等

## 2. 功能分解

### 2.1 考勤记录模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 班级选择 | 下拉选择当前教师负责的班级 | P0 |
| 课次选择 | 选择当前课次或历史课次 | P0 |
| 学生列表展示 | 显示班级所有学生，支持头像+姓名 | P0 |
| 快速签到 | 一键标记出勤/缺勤/请假/迟到 | P0 |
| 全员签到 | 一键将所有学生标记为出勤 | P1 |
| 备注功能 | 对特殊情况添加备注 | P2 |

### 2.2 考勤查询模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 按日期查询 | 查看某天的所有考勤记录 | P0 |
| 按班级查询 | 查看某班级的考勤历史 | P0 |
| 按学生查询 | 查看某个学生的出勤情况 | P1 |
| 导出报表 | 导出Excel考勤报表 | P2 |

### 2.3 统计分析模块
| 功能 | 描述 | 优先级 |
|------|------|--------|
| 班级出勤率 | 各班级的整体出勤率 | P0 |
| 个人出勤率 | 单个学生的出勤统计 | P1 |
| 缺勤预警 | 缺勤次数超过阈值的学生提醒 | P1 |
| 趋势图表 | 出勤率变化趋势图 | P2 |

## 3. 技术架构

### 3.1 模块目录结构
```
src/attendance/
├── CMakeLists.txt              # 模块构建配置
├── models/
│   ├── AttendanceRecord.h      # 考勤记录模型
│   ├── AttendanceRecord.cpp
│   ├── AttendanceStatus.h      # 考勤状态枚举
│   └── AttendanceSummary.h     # 考勤统计汇总模型
├── services/
│   ├── AttendanceService.h     # 考勤服务层
│   └── AttendanceService.cpp
└── ui/
    ├── AttendanceWidget.h      # 考勤主界面
    ├── AttendanceWidget.cpp
    ├── AttendanceItemDelegate.h  # 学生列表项代理（自定义渲染）
    ├── AttendanceItemDelegate.cpp
    ├── AttendanceStatsWidget.h   # 统计图表组件
    └── AttendanceStatsWidget.cpp
```

### 3.2 数据模型设计

#### AttendanceStatus 枚举
```cpp
enum class AttendanceStatus {
    Present = 0,    // 出勤
    Absent = 1,     // 缺勤
    Late = 2,       // 迟到
    Leave = 3,      // 请假
    EarlyLeave = 4  // 早退
};
```

#### AttendanceRecord 模型
```cpp
class AttendanceRecord {
    int m_id;                      // 记录ID
    int m_studentId;               // 学生ID
    int m_classId;                 // 班级ID
    QString m_courseId;            // 课程ID
    int m_lessonNumber;            // 课次（第几节课）
    QDate m_date;                  // 考勤日期
    QTime m_checkTime;             // 签到时间
    AttendanceStatus m_status;     // 考勤状态
    QString m_remark;              // 备注
    QString m_recorderId;          // 记录人ID（教师）
    QDateTime m_createdAt;         // 创建时间
    QDateTime m_updatedAt;         // 更新时间
};
```

#### AttendanceSummary 统计模型
```cpp
class AttendanceSummary {
    int m_totalCount;       // 总应到人次
    int m_presentCount;     // 出勤人次
    int m_absentCount;      // 缺勤人次
    int m_lateCount;        // 迟到人次
    int m_leaveCount;       // 请假人次
    double m_attendanceRate; // 出勤率
};
```

### 3.3 数据库表设计（Supabase）

```sql
-- 考勤记录表
CREATE TABLE attendance_records (
    id SERIAL PRIMARY KEY,
    student_id INTEGER NOT NULL REFERENCES students(id),
    class_id INTEGER NOT NULL REFERENCES course_classes(id),
    course_id VARCHAR(50),
    lesson_number INTEGER NOT NULL,
    attendance_date DATE NOT NULL,
    check_time TIME,
    status INTEGER NOT NULL DEFAULT 0,
    remark TEXT,
    recorder_id VARCHAR(50) NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),

    -- 唯一约束：同一学生同一天同一课次只能有一条记录
    UNIQUE(student_id, attendance_date, lesson_number)
);

-- 创建索引优化查询
CREATE INDEX idx_attendance_class_date ON attendance_records(class_id, attendance_date);
CREATE INDEX idx_attendance_student ON attendance_records(student_id);
CREATE INDEX idx_attendance_recorder ON attendance_records(recorder_id);
```

### 3.4 服务层接口设计

```cpp
class AttendanceService : public QObject {
    Q_OBJECT
public:
    // 获取班级学生列表（复用analytics模块）
    void fetchStudentsByClass(int classId);

    // 获取某课次的考勤记录
    void fetchAttendanceByLesson(int classId, const QDate &date, int lessonNumber);

    // 批量提交考勤记录
    void submitAttendance(const QList<AttendanceRecord> &records);

    // 更新单条考勤记录
    void updateAttendance(const AttendanceRecord &record);

    // 获取班级考勤统计
    void fetchClassStatistics(int classId, const QDate &startDate, const QDate &endDate);

    // 获取学生考勤统计
    void fetchStudentStatistics(int studentId, const QDate &startDate, const QDate &endDate);

signals:
    void studentsReceived(const QList<Student> &students);
    void attendanceReceived(const QList<AttendanceRecord> &records);
    void attendanceSubmitted(bool success);
    void statisticsReceived(const AttendanceSummary &summary);
    void errorOccurred(const QString &error);
};
```

## 4. UI 设计要点

### 4.1 整体布局
```
┌──────────────────────────────────────────────────────────┐
│  🎯 考勤管理                                    [统计] [导出] │
├──────────────────────────────────────────────────────────┤
│  班级: [下拉选择▼]    日期: [日期选择]    课次: [1▼]         │
├──────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐  │
│  │  [全员签到]  出勤: 35  缺勤: 2  迟到: 1  请假: 2    │  │
│  └────────────────────────────────────────────────────┘  │
├──────────────────────────────────────────────────────────┤
│  ┌──────┬─────────────┬────────────────────┬─────────┐   │
│  │ 头像 │ 姓名/学号   │ 考勤状态           │ 备注    │   │
│  ├──────┼─────────────┼────────────────────┼─────────┤   │
│  │ [王] │ 王小明      │ [✓出勤] [✗] [迟] [假] │ [备注]  │   │
│  │ [李] │ 李小红      │ [✓出勤] [✗] [迟] [假] │ [备注]  │   │
│  │ [张] │ 张小刚      │ [✓] [✗缺勤] [迟] [假] │ 生病    │   │
│  │ ... │ ...         │ ...                │ ...     │   │
│  └──────┴─────────────┴────────────────────┴─────────┘   │
├──────────────────────────────────────────────────────────┤
│                    [保存考勤记录]                          │
└──────────────────────────────────────────────────────────┘
```

### 4.2 设计规范
- **主色调**：延续项目的"中国红"爱国主题 (#D32F2F)
- **卡片风格**：白色背景、圆角12px、阴影效果
- **状态颜色**：
  - 出勤：绿色 #4CAF50
  - 缺勤：红色 #F44336
  - 迟到：橙色 #FF9800
  - 请假：蓝色 #2196F3
  - 早退：紫色 #9C27B0
- **交互反馈**：点击状态按钮有即时视觉反馈

### 4.3 响应式适配
- 支持窗口大小变化
- 学生列表支持滚动
- 移动端适配（如果未来需要）

## 5. 实施步骤

### 第一阶段：基础框架搭建（P0）
1. [ ] 创建 `src/attendance/` 目录结构
2. [ ] 实现 `AttendanceRecord` 和 `AttendanceStatus` 数据模型
3. [ ] 实现 `AttendanceService` 服务层（CRUD操作）
4. [ ] 创建 Supabase 数据库表
5. [ ] 在 `ModernMainWindow` 侧边栏添加"考勤管理"入口

### 第二阶段：UI设计（P0）⚠️ 必须先完成设计再开发
> **老王说：前端不先出设计稿就开干，那是SB行为！**

6. [ ] 🎨 **调用 `/frontend-design` skill** 生成考勤主界面设计
   - 输入：功能需求、项目现有风格（中国红主题）
   - 输出：AttendanceWidget 完整UI设计方案
7. [ ] 🎨 **调用 `/ui-ux-designer` agent** 优化交互设计
   - 学生列表项的交互细节
   - 状态切换按钮的视觉反馈
   - 响应式布局适配方案

### 第三阶段：核心UI开发（P0）
8. [ ] 根据设计稿实现 `AttendanceWidget` 主界面
9. [ ] 实现班级/日期/课次选择器
10. [ ] 实现学生列表展示（使用 QListView + 自定义 Delegate）
11. [ ] 实现考勤状态切换按钮组
12. [ ] 实现批量保存功能

### 第四阶段：增强功能（P1）
13. [ ] 实现考勤历史查询
14. [ ] 实现个人出勤统计
15. [ ] 实现缺勤预警提醒
16. [ ] 对接通知系统（缺勤通知）

### 第五阶段：统计报表UI设计与开发（P2）
17. [ ] 🎨 **调用 `/frontend-design` skill** 生成统计图表界面设计
18. [ ] 实现 `AttendanceStatsWidget` 统计组件
19. [ ] 实现出勤率趋势图表（复用 analytics 的图表组件）
20. [ ] 实现 Excel 导出功能

---

## 5.1 UI设计Skill调用说明

### 主界面设计调用
```
/frontend-design
- 组件类型: Qt Widgets (QFrame/QListView/QPushButton)
- 设计风格: 延续项目"中国红"爱国主题
- 参考组件: NotificationWidget、DataAnalyticsWidget
- 核心功能: 班级选择器、学生考勤列表、状态切换按钮组
```

### UI/UX优化调用
```
使用 ui-ux-designer agent 处理:
1. 状态按钮组的交互反馈设计
2. 学生列表项hover/selected状态
3. 批量操作的确认交互流程
4. 加载状态和错误提示的UX设计
```

## 6. 验收标准

### 6.1 功能验收
- [ ] 能够选择班级并加载学生列表
- [ ] 能够对每个学生进行考勤状态标记
- [ ] 能够批量保存考勤记录
- [ ] 能够查看历史考勤记录
- [ ] 考勤数据能够正确写入 Supabase

### 6.2 性能验收
- [ ] 学生列表加载时间 < 2秒（50人班级）
- [ ] 考勤保存响应时间 < 3秒
- [ ] 界面切换流畅无卡顿

### 6.3 UI验收
- [ ] 界面风格与现有系统一致
- [ ] 状态按钮交互反馈明确
- [ ] 适配不同窗口大小

## 7. 风险与依赖

### 7.1 依赖项
- 复用 `analytics/models/Student.h` 和 `CourseClass.h`
- 复用 `SupabaseClient` 进行数据库操作
- 复用 `NotificationService` 发送缺勤通知

### 7.2 潜在风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Supabase 网络延迟 | 保存响应慢 | 添加本地缓存，异步保存 |
| 大班级数据量大 | 列表渲染慢 | 使用虚拟化列表（QListView） |
| 数据冲突 | 多端同时操作 | 添加乐观锁机制 |

## 8. 时间规划

> 老王说：别问老王多久能干完，干完了自然就完了！

- 第一阶段：基础框架 → 预计 1-2 天
- 第二阶段：核心UI → 预计 2-3 天
- 第三阶段：增强功能 → 预计 1-2 天
- 第四阶段：统计报表 → 预计 1-2 天

---

**文档版本**: v1.0
**创建日期**: 2026-02-03
**作者**: 老王

> 艹，规划完了！这个考勤功能其实不复杂，核心就是：选班级 → 点名 → 保存。
> 等你确认了老王就开始干活！
